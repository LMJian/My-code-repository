题目内容：
二维数组中的鞍点，即该位置上的元素是所在行上的最小值，也是所在列上的最大值。
二维数组也可能没有鞍点。
输入一个二维数组的行数m，列数n，二维数组的诸元素值；输出数组鞍点的所在的行列号。
输入格式：
第一行为矩阵的行数和列数，从第二行开始，为矩阵本身（输入的矩阵只有0和1个鞍点）
输出格式：
数组鞍点的所在行列号 (下标从1开始)
输入样例：
4 3
3 1 7
5 4 6
3 3 2
1 2 3
输出样例：
a【2】【2】

代码
#include<stdio.h>

int main()
{
	int a[99][99]={0};
	int m,n,i,j,k,t;
	scanf("%d%d",&m,&n);
	for(i=1;i<=m;i++)
	for(j=1;j<=n;j++)
	scanf("%d",&a[i][j]);
	for(i=1;i<=m;i++)
	{
		int x=i,y=1;
		for(j=2;j<=n;j++)
		if(a[x][y]>a[i][j])
		y=j;
		t=1;
		for(k=1;k<=m;k++)
		if(a[k][y]>a[x][y])
		t=0;
		if(t==1)
		{printf("x[%d][%d]\n",x,y);
		break;}
	}
	if(t==0)
	printf("没有鞍点"); 
	return 0;
}
